# 考研学习助手 · 网页版部署说明

目标：让**任意同学用浏览器**打开本应用（电脑端 / 手机端均可），AI 功能（千问识图、DeepSeek 解答）在公网也能用。

整体结构：
- **静态站**：`www/` 目录直接发布到 GitHub Pages（免费）。
- **AI 代理**：一个免费的 Cloudflare Worker，专门转发浏览器 → 千问/DeepSeek 的请求（绕开 CORS）。
- **数据/登录/金币**：沿用 Bmob 云，天然支持多用户，无需改动。
- **安卓 App（APK）**：完全不受影响，仍直连 Bmob 与 AI。

---

## 一、准备 Cloudflare Worker（AI 代理）

> 只需做一次。Worker 免费额度足够个人/小团队使用。

1. 注册 Cloudflare 账号（免费）：https://dash.cloudflare.com/
2. 本地安装 Wrangler：
   ```bash
   npm install -g wrangler
   ```
3. 登录并授权：
   ```bash
   wrangler login
   ```
4. 进入仓库内 `worker/` 目录部署：
   ```bash
   cd worker
   wrangler deploy
   ```
   部署成功后命令行会输出一个地址，形如：
   ```
   https://kaoyan-ai-proxy.<你的子域>.workers.dev
   ```
   记下这个地址（下面要用）。

> 不想用命令行也可以：在 Cloudflare 控制台 **Workers & Pages → Create → 粘贴 `worker/index.js` 内容 → 部署**，效果相同。

---

## 二、填入代理地址

编辑 `www/js/config.js`，把 `proxyUrl` 改成刚才的 Worker 地址（注意带上 `/api/proxy`）：

```js
window.APP_CONFIG = window.APP_CONFIG || {};
window.APP_CONFIG.proxyUrl = 'https://kaoyan-ai-proxy.<你的子域>.workers.dev/api/proxy';
```

- 本地调试时保持 `'/api/proxy'`（由项目根目录 `serve.py` 提供）即可。
- **部署到公网前务必改成绝对地址**，否则网页端 AI 功能无法使用。

---

## 三、发布静态站到 GitHub Pages

1. 打开仓库 **Settings → Pages → Build and deployment → Source**，选择 **GitHub Actions**。
2. 把改动推送到 `main` 分支（或到 Actions 页面手动运行 `Deploy Web (GitHub Pages)`）。
3. 稍等 1~2 分钟，访问：
   ```
   https://Starlit-Voyager-ship.github.io/kaoyan-app/
   ```
   把这个链接发给同学即可。

> 触发条件：只有 `www/**` 或部署工作流本身变动时才会重新发布，不会每次都跑。

---

## 四、本地调试

```bash
cd 考研学习助手
python serve.py
# 浏览器打开 http://localhost:8765
```
本地默认 `proxyUrl='/api/proxy'`，AI 请求由 `serve.py` 在本机转发，无需 Worker。

---

## 五、安全提醒

- **AI Key**：每个用户在「设置」里填自己的 Key，代理只转发、不存储，Key 不会落到服务端。
- **GitHub remote 含明文 Token**：当前仓库 remote URL 里带有 `ghp_...` 明文令牌（见 `git remote -v`）。建议到 GitHub **Settings → Developer settings → Personal access tokens** 将其 **Revoke** 并重新生成，再更新 remote，避免令牌泄露风险。
- Cloudflare Worker 默认对所有人开放转发，仅做透传、不暴露 Key；若担心被滥用，可在 `worker/index.js` 里对来源 `request.headers.get('origin')` 做白名单限制（只允许你的 Pages 域名）。
