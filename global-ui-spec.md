# 考研学习助手 · 全局 UI 设计规范（Global UI Spec）

> 版本：v2026-08-02g｜基于导航重构（v=y）+ 既有硬约束收口  
> 作用：统一双端（Web `www/` + Android `www/`）全部页面视觉，消除不协调/廉价感。  
> 本规范是所有页面、组件、样式的唯一权威来源；代码改动以本文件为准。  
> 色彩以 **HEX 为基准值**，OKLCH 供现代浏览器直接调用（无 OKLCH 环境回退 HEX）。

---

## 0. 设计原则（三条，违反即返工）

1. **安静沉稳，不抢戏**：淡紫主色全程纯色平涂、零渐变；禁止任何高饱和刺眼色（荧光绿/亮红/电蓝一律否决）。
2. **线性一致，零 Emoji**：所有图标统一粗细线性 SVG；任何原生 Emoji 视为缺陷，必须清除。
3. **层级清晰，留白有章**：每一屏有且只有一个视觉重心；间距、圆角、阴影全部走 Token，禁止页面级硬编码魔法值。

---

## 1. 色彩系统（Color Tokens）

### 1.1 主色 / 功能色（语义色）

| Token                | HEX       | OKLCH                  | 用途                            |
| -------------------- | --------- | ---------------------- | ----------------------------- |
| `--c-primary`        | `#6E5CA8` | `oklch(0.49 0.09 295)` | 主色（薰衣草）。按钮、选中态、强调、Tab 激活、图标主色 |
| `--c-primary-soft`   | `#ECE8F5` | `oklch(0.93 0.02 295)` | 浅薰衣草底。选中项背景、标签底、弱强调区          |
| `--c-primary-strong` | `#5A4889` | `oklch(0.42 0.09 295)` | 主色按下/深强调。按钮 active、重要数字       |
| `--c-primary-tint`   | `#F5F2FA` | `oklch(0.96 0.01 295)` | 主色极浅底。卡片分区、空状态底               |

### 1.2 中性色（浅色主题页面）

| Token               | HEX       | OKLCH                   | 用途               |
| ------------------- | --------- | ----------------------- | ---------------- |
| `--c-bg`            | `#F7F7FA` | `oklch(0.97 0.003 280)` | 页面背景（App 底）      |
| `--c-surface`       | `#FFFFFF` | —                       | 卡片 / 浮层 / 输入底    |
| `--c-surface-2`     | `#FBFBFD` | `oklch(0.99 0.002 280)` | 次级表面（列表行、内嵌区）    |
| `--c-text-1`        | `#2A2730` | `oklch(0.25 0.012 300)` | 一级文字（标题、正文、数字）   |
| `--c-text-2`        | `#6B6775` | `oklch(0.46 0.012 300)` | 二级文字（副标题、说明）     |
| `--c-text-3`        | `#9A95A6` | `oklch(0.61 0.012 300)` | 三级文字（备注、辅助小字、占位） |
| `--c-border`        | `#ECEAF0` | `oklch(0.95 0.004 300)` | 分割线、卡片描边、输入框线    |
| `--c-border-strong` | `#DCD8E4` | `oklch(0.90 0.006 300)` | 强分割线（分组边界）       |

### 1.3 功能状态色（全部低饱和柔和）

| Token         | HEX       | OKLCH                  | 用途              |
| ------------- | --------- | ---------------------- | --------------- |
| `--c-danger`  | `#C25B6B` | `oklch(0.55 0.11 12)`  | 危险/删除/错误（柔和玫红）  |
| `--c-success` | `#5E9B7E` | `oklch(0.60 0.07 155)` | 完成/正确（柔和草绿）     |
| `--c-warning` | `#C99A4E` | `oklch(0.66 0.09 75)`  | 提醒/待办（柔和琥珀）     |
| `--c-gold`    | `#B8923E` | `oklch(0.58 0.08 80)`  | 金币/积分（低饱和金，禁亮黄） |

> 状态色仅用于语义标记（对勾/红点/进度异常），不得作为大面积装饰底色。

### 1.4 侧边抽屉深色主题（独立暗色面板，不随页面浅色主题切换）

| Token              | HEX       | OKLCH                   | 用途              |
| ------------------ | --------- | ----------------------- | --------------- |
| `--drawer-bg`      | `#211E2B` | `oklch(0.20 0.012 300)` | 抽屉整体背景（深紫灰，非死黑） |
| `--drawer-surface` | `#2C2838` | `oklch(0.27 0.012 300)` | 抽屉内分组块/卡片底      |
| `--drawer-active`  | `#3A3550` | `oklch(0.30 0.03 295)`  | 当前选中项背景         |
| `--drawer-text-1`  | `#F2F0F6` | `oklch(0.95 0.006 300)` | 抽屉一级文字（项标题）     |
| `--drawer-text-2`  | `#A8A2B8` | `oklch(0.65 0.012 300)` | 抽屉二级文字（说明）      |
| `--drawer-text-3`  | `#6E687E` | `oklch(0.48 0.012 300)` | 抽屉分组标签 / 三级     |
| `--drawer-divider` | `#383444` | `oklch(0.32 0.012 300)` | 分组分割线           |

---

## 2. 圆角规范（Radius）

| Token      | 值       | 用途               |
| ---------- | ------- | ---------------- |
| `--r-sm`   | `8px`   | 小标签、输入控件、图标按钮    |
| `--r-md`   | `12px`  | **卡片默认圆角（全局统一）** |
| `--r-lg`   | `16px`  | 大卡片、弹层、底部 Sheet  |
| `--r-pill` | `999px` | 胶囊标签、头像、开关       |
| `--r-icon` | `10px`  | 图标按钮底            |

> 强制：所有卡片统一 `--r-md`(12px)，禁止页面级出现 6px/14px/20px 等非标圆角。

---

## 3. 阴影规范（Shadow · 柔和浅阴影）

| Token             | 值                                                              | 用途            |
| ----------------- | -------------------------------------------------------------- | ------------- |
| `--sh-card`       | `0 1px 3px rgba(42,39,48,0.06), 0 1px 2px rgba(42,39,48,0.04)` | 卡片静态阴影（极轻）    |
| `--sh-card-hover` | `0 4px 12px rgba(110,92,168,0.10)`                             | 卡片 hover/可点击态 |
| `--sh-fab`        | `0 6px 16px rgba(110,92,168,0.28)`                             | 悬浮按钮（FAB）     |
| `--sh-drawer`     | `4px 0 24px rgba(0,0,0,0.18)`                                  | 侧边抽屉右缘投影      |
| `--sh-pop`        | `0 8px 28px rgba(42,39,48,0.14)`                               | 弹层/下拉菜单       |

> 阴影仅用黑/主色低透明度柔光，禁止硬黑重影、禁止彩色发光渐变。

---

## 4. 间距与留白规范（Spacing · 4px 基线栅格）

### 4.1 间距阶梯（Token = 4px × n）

| Token    | px | 典型用途                   |
| -------- | -- | ---------------------- |
| `--s-1`  | 4  | 图标与文字最小间隙              |
| `--s-2`  | 8  | 控件内元素间距、标签间距           |
| `--s-3`  | 12 | 卡片内区块间距、列表行内 gap       |
| `--s-4`  | 16 | **页面左右内边距 / 卡片内边距基准**  |
| `--s-5`  | 20 | 卡片标题与内容间距              |
| `--s-6`  | 24 | **区块（section）之间的垂直间距** |
| `--s-8`  | 32 | 大区块留白、页头底部             |
| `--s-10` | 40 | 首屏顶部留白                 |
| `--s-12` | 48 | 页面级大留白 / 空状态上下         |

### 4.2 页面与容器留白（全局标准化）

- **页面左右内边距**：`--s-4`(16px)，移动端全宽；Web 端内容最大宽度 `720px` 居中。
- **页面顶部留白**：`--s-6`(24px)（页头之下）；首屏可到 `--s-10`(40px)。
- **页面底部留白**：`--s-8`(32px)，确保不被底部 Tab 遮挡。
- **卡片内边距**：`--s-4`(16px) 统一；信息密集卡可用 `--s-4` 上下 + `--s-4` 左右，禁止出现 10/14/18px 等非标。
- **卡片之间的间距**：`--s-3`(12px)（纵向列表）/ `--s-4`(16px)（网格）。
- **区块标题与内容间距**：`--s-3`(12px)。

---

## 5. 字体与排版层级（Typography）

### 5.1 字体栈（全局统一，禁用 Inter/Roboto/Arial）

```css
--font-sans: "PingFang SC", "Microsoft YaHei", "Noto Sans SC",
             "Source Han Sans SC", sans-serif;
```

### 5.2 三层核心层级（用户明确要求）+ 扩展刻度

| 层级           | Token         | 字号   | 字重  | 行高  | 字色                | 用途             |
| ------------ | ------------- | ---- | --- | --- | ----------------- | -------------- |
| **标题（大号加粗）** | `--t-title`   | 20px | 600 | 1.3 | `--c-text-1`      | 页面主标题、卡片大标题    |
| **正文（常规）**   | `--t-body`    | 14px | 400 | 1.6 | `--c-text-1`/`-2` | 段落、列表、说明正文     |
| **辅助（浅灰小字）** | `--t-caption` | 12px | 400 | 1.5 | `--c-text-3`      | 备注、时间戳、占位、次级说明 |

### 5.3 扩展刻度（保持连续梯度，不跳号）

| Token         | 字号   | 字重  | 行高  | 用途                      |
| ------------- | ---- | --- | --- | ----------------------- |
| `--t-display` | 28px | 700 | 1.2 | 首页核心数据大数字（tabular-nums） |
| `--t-h2`      | 16px | 600 | 1.4 | 区块标题、卡片标题               |
| `--t-body-lg` | 15px | 400 | 1.6 | 重点正文                    |
| `--t-label`   | 11px | 500 | 1.4 | 底部 Tab 文字、标签、抽屉分组名      |

> 数字类（统计、金币、计时）统一 `font-variant-numeric: tabular-nums;` 防止跳动。  
> 字间距：大标题/数字 `letter-spacing: -0.01em`；其余默认。

---

## 6. 图标规范（Icon · 线性 SVG 标准）

### 6.1 统一绘制标准（所有图标必须遵守）

```svg
![svg](data:image/svg+xml;base64,PHN2ZyB2aWV3Qm94PSIwIDAgMjQgMjQiIGZpbGw9Im5vbmUiCiAgICAgc3Ryb2tlPSJjdXJyZW50Q29sb3IiIHN0cm9rZS13aWR0aD0iMS43NSIKICAgICBzdHJva2UtbGluZWNhcD0icm91bmQiIHN0cm9rZS1saW5lam9pbj0icm91bmQiIGhlaWdodD0iMjQiPgogIAo8L3N2Zz4=)
```

- `fill="none"`、`stroke="currentColor"`（颜色随文字色走，禁止写死色值）
- `stroke-width="1.75"` 全局统一，禁止 1/1.5/2 混用
- `viewBox="0 0 24 24"`，禁止其他比例
- 线端/转角 `round`

### 6.2 图标尺寸阶梯

| 场景             | 尺寸      | 备注                   |
| -------------- | ------- | -------------------- |
| 行内小图标（列表项、按钮内） | 20px    | 默认                   |
| 底部 Tab 图标      | 24px    | 激活态可加 `--c-primary`  |
| 空状态 / 插画图标     | 28–48px | 单色线性，可配 `--c-text-3` |
| 操作小图（关闭、更多）    | 16px    | 仅限紧凑控件               |

### 6.3 Emoji 清除清单（硬性）

- 全站 `www/` 检索并替换所有原生 Emoji（含宠物商店 🍖、状态 😊、空状态 ❓ 等）为对应线性 SVG。
- 替换优先级：导航 chrome → 首页统计 → 空状态 → 模块内装饰（D 项遗留）。
- 验收：页面渲染后无任何彩色 Emoji 字形；用字体回退方框也为缺陷。

---

## 7. 卡片通用组件规范（Card）

### 7.1 基础卡

```css
.card {
  background: var(--c-surface);
  border-radius: var(--r-md);        /* 12px */
  padding: var(--s-4);               /* 16px */
  box-shadow: var(--sh-card);
  border: 1px solid transparent;
}
```

- 内部分区：标题(`--t-h2`) → 内容(`--t-body`) 间距 `--s-3`(12px)；内容 → 底部操作 `--s-3`(12px)。
- 可点击卡：`cursor:pointer`；hover → `--sh-card-hover` + `transform: translateY(-1px)`，`transition: .18s ease`。

### 7.2 卡片状态（统一）

| 状态             | 表现                                             |
| -------------- | ---------------------------------------------- |
| default        | 白底 + `--sh-card`                               |
| hover          | `--sh-card-hover` + 上浮 1px                     |
| active/pressed | 底色 `--c-primary-tint`，图标/数字转 `--c-primary`     |
| selected       | 左侧 3px `--c-primary` 竖条 + `--c-primary-soft` 底 |
| disabled       | 整体 `opacity:.5`，禁止指针                           |

### 7.3 卡片变体

- **统计卡（首页）**：大数字 `--t-display` + 标签 `--t-caption`，左上小线性图标 20px `--c-primary`。
- **列表卡**：行高 48px，左图标 + 中标题(`--t-body`)/副(`--t-caption`) + 右箭头/状态。
- **分区卡**：内部用 `--c-border` 1px 线再分，禁止再加阴影嵌套。

---

## 8. 底部导航 Tab 规范（Bottom Nav）

- 顺序锁死：**首页概览 / 专注计时 / AI助理 / 学习报表 / 我的**（不可改）。
- 高度 `56px` + 顶部 1px `--c-border`；背景 `--c-surface`，`position:fixed` 底部。
- 每项：图标 24px（默认 `--c-text-3`，激活 `--c-primary`）+ 文字 `--t-label`(11px，默认 `--c-text-3`，激活 `--c-primary`)。
- 激活态：图标与文字同色 `--c-primary`，无背景块、无圆点，靠颜色区分（保持扁平安静）。
- 中部「AI助理」可略放大图标（26px）作轻微视觉锚点，但不得用色块/渐变底。

---

## 9. 侧边抽屉规范（Drawer · 深色优化）

### 9.1 容器

- 宽度 `280px`，背景 `--drawer-bg`，右缘 `--sh-drawer`。
- 顶部安全留白 `--s-6`(24px)；整体右侧留 1px `--drawer-divider` 收边。

### 9.2 头部

- 应用名（如「考研学习助手」）`--t-h2`(16px/600) `--drawer-text-1`，左留 logo 线性图标 24px `--c-primary`。
- 右上关闭按钮：图标 20px `--drawer-text-2`，hover 转 `--drawer-text-1`。

### 9.3 分组与分割线（重点优化）

- 三个分组：**英语专项 / 数学专项 / 拓展**。
- 每个分组**顶部一条 1px `--drawer-divider` 分割线**，线上方留 `--s-5`(20px) 间距。
- 分组标签：文字 `--t-label`(11px/500) `--drawer-text-3`，可加字距 `0.08em`，不衬底色。
- 组内项：高度 `--s-12`≈48px，内边距 `12px 16px`；左图标 20px + 标题 `--t-body`(14px) `--drawer-text-1`。

### 9.4 抽屉项状态与文字层级

| 层级         | 字号/色                   | 示例              |
| ---------- | ---------------------- | --------------- |
| 项标题（一级）    | 14px `--drawer-text-1` | 单词背诵、题库         |
| 项说明（二级，可选） | 12px `--drawer-text-2` | 已学 320 / 待复习 12 |
| 分组标签（三级）   | 11px `--drawer-text-3` | 英语专项            |

- 选中项：背景 `--drawer-active` + 左 3px `--c-primary` 竖条 + 标题转 `--c-primary`(提亮)。
- hover：背景 `rgba(255,255,255,0.04)`，标题转 `--drawer-text-1`。
- 置灰项（即将上线）：`opacity:.45`，右侧 `--t-caption` 标签「即将上线」`--drawer-text-3`。

### 9.5 抽屉底部

- 1px `--drawer-divider` 上方留 `--s-5`(20px)；放用户/设置入口（图标+文字 `--drawer-text-2`）。

---

## 10. 首页页面重心微调方案（Homepage Focus Tuning）

### 10.1 现状问题（重构前）

- 四个统计卡（专注/背词/录题/金币）等权并排，**无主次**，首屏被数字铺满、无呼吸感。
- 快捷入口图标风格不一、部分仍 Emoji，与整体线性调性冲突。
- 缺少「今天该做什么」的引导重心，用户进首页不知从哪开始。

### 10.2 目标重心

**一屏一个视觉锚点**：把「今日专注时长」作为绝对重心（大数字卡），其余统计降为次级，快捷入口降为工具行，新增「继续学习」引导卡承接行动。

### 10.3 新首页布局（自上而下，含间距）

```
┌─────────────────────────────────────┐
│ [安全区 24px]                        │
│ 你好，考研人 👋→(线性太阳图标)  日期  │  ← 页头：问候(--t-h2) + 日期(--t-caption)
│                                      │
│  ┌─────────────────────────────┐    │
│  │ 🕒→(时钟线性) 今日专注        │    │  ← 重心卡（hero）
│  │     2h 35m  (--t-display 28px)│    │     底色 --c-primary-soft
│  │     较昨日 +18min (--t-caption)│   │     大数字 --c-primary-strong
│  └─────────────────────────────┘    │
│ [间距 12px]                         │
│  ┌────┐ ┌────┐ ┌────┐              │  ← 次级统计：3 等分卡
│  │背词 │ │录题 │ │金币 │            │     数字 --t-h2(16px/700)
│  │120  │ │ 8  │ │340 │            │     标签 --t-caption
│  └────┘ └────┘ └────┘              │
│ [区块间距 24px]                     │
│ 继续学习  (--t-h2 区块标题)         │
│  ┌─────────────────────────────┐    │
│  │ 线性图标 线性代数·矩阵 进行中 │    │  ← 引导卡：承接行动
│  │ 进度 60%   [继续 →]          │    │     进度条 --c-primary
│  └─────────────────────────────┘    │
│ [区块间距 24px]                     │
│ 快捷入口  (--t-h2 区块标题)         │
│  [图标] [图标] [图标] [图标] [图标] │  ← 线性图标工具行，5 项等分
│   专注   背词   AI   报表   我的     │
│ [底部留白 32px]                     │
└─────────────────────────────────────┘
```

### 10.4 具体改动清单（前端实现）

1. **页头**：问候语「你好，考研人」用 `--t-h2`；原 Emoji 换成线性「太阳/月亮」图标 20px `--c-primary`；右侧日期 `--t-caption`。
2. **重心卡（hero）**：独占一行，底色 `--c-primary-soft`，左 20px 时钟线性图标 `--c-primary`；大数字 `--t-display`(28px/700) 色 `--c-primary-strong`，下方对比 `--t-caption`。此卡不与其他统计卡同排。
3. **次级统计**：原 4 卡 → 降为 3 卡（背词/录题/金币），`grid` 三等分，卡内数字 `--t-h2`(16px/700) `--c-text-1`，标签 `--t-caption`；去掉「专注」卡（已升为 hero）。
4. **继续学习引导卡**：新增，左线性图标 + 模块名(`--t-body`)，右进度条（高 6px、`--c-primary` 填充、`--c-border` 轨道）+「继续 →」文字按钮(`--c-primary`)。
5. **快捷入口**：统一 5 个线性图标（替换残留 Emoji），图标 24px `--c-text-2`、激活/`hover` 转 `--c-primary`；下方文字 `--t-label`(11px) `--c-text-2`。
6. **留白**：严格按 §4.2——页头下 24px、区块间 24px、卡间 12px、底部 32px。
7. **禁止**：首页不得出现渐变底、不得出现彩色大色块、不得出现 Emoji、统计数字不得用跳号字号。

### 10.5 重心微调验收点

- 进首页 0.5 秒内视线先落在「今日专注」大数字卡（唯一 28px 元素）。
- 全屏无两种以上字号抢戏；层级为 28 → 16 → 14 → 12 单调降序。
- 无任何 Emoji；所有图标线性 1.75 统一。

---

## 11. 落地检查清单（前端核对用）

- [ ] 所有页面引用同一套 CSS 变量（`:root` 注入 `www/css/main.css`），无页面级魔法值。
- [ ] 全站 Emoji 清零（grep `[\x{1F300}-\x{1FAFF}]` + 常见符号 Emoji）。
- [ ] 图标统一 `stroke-width:1.75` + `viewBox 0 0 24 24` + `fill:none`。
- [ ] 卡片圆角全部 `--r-md`(12px)；阴影仅用 §3 Token。
- [ ] 抽屉深色背景 + 分组分割线 + 三级文字层级落实（§9）。
- [ ] 字体栈无 Inter/Roboto/Arial；数字 `tabular-nums`。
- [ ] 间距全部走 §4 Token；页面内边距 16px、区块间距 24px。
- [ ] 主色仅 `#6E5CA8` 及其深浅变体，零渐变。
- [ ] 改完 JS/CSS 后 `node --check` + 浏览器 Ctrl+Shift+R 强刷验证。

---

> 本规范与 `navigation-redesign-spec.md` 互补：导航文档管「结构/入口」，本文档管「视觉/Token」。两者冲突以本文档 Token 值为准。
