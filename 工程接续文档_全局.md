# 考研学习助手 · 工程接续文档（全局 · 维护/排障向）

> 新会话读取本文后，无需重新理解需求，可直接从 ⑥ 接续引导 的定位继续开发或排障。
> 配套子文档：`工程接续文档_英语AI识别.md`（英语识别专项，部分内容已被本文覆盖/更新）。
> 全局 UI 规范权威源：`global-ui-spec.md`。

---

## ① 项目总览

### 定位
- 手机（Capacitor Android APK）+ 电脑（Web / GitHub Pages）**双端互通**的考研学习软件。
- 核心卖点：背单词 / 数学题库 / 英语阅读AI识别 / AI答疑 / 桌面宠物 / 好友叫醒 / 番茄专注 / 学习报表。
- 云同步：Bmob（本地 IndexedDB + 云端 AppData 双写）。
- 代码仓库：`Starlit-Voyager-ship/kaoyan-app`（注意 git remote 含明文 token，交付后务必 Revoke）。
- 本地工程：`C:/Users/yansh/WorkBuddy/考研软件/考研学习助手`
- 桌面 APK 产出：`C:/Users/yansh/Desktop/kaoyan-study-helper-debug.apk`
- 最新可用构建：commit `8efc803`（好友叫醒双向互绑），APK 已落地桌面。

### 技术栈
- 前端：纯 HTML/CSS/JS（无框架），`www/` 目录，Capacitor 包装成 Android APK。
- 后端：Bmob（云数据库 + 账号体系 + 云函数 `aiProxy` 代理千问）。
- AI：文本 `qwen-max` + 视觉 `qwen-vl-max`，经 Bmob 云函数 `aiProxy`（浏览器受 40KB 限制）或直接原生 fetch（App 内 220KB 预算）。
- 原生 Android 插件：`CameraPlugin`（拍照/相册）、`Wake*`（叫醒前台守护/响铃震动/锁屏页/停止广播）。
- 公式渲染：KaTeX（CDN 引入）。

### 模块清单（页面 ↔ JS ↔ 云端表）
| 页面(section) | 入口 | 主文件 | 云端表（Store键） | 状态 |
|---|---|---|---|---|
| 首页概览 home | tab/主页 | app.js + 各模块挂载 | — | ✅ |
| AI助理 ai-assistant | tab | ai-assistant.js | ai_chats | ✅（答题占位除外）|
| 专注计时 pomodoro | tab | pomodoro.js | pomodoro_records/pomo_tasks/pomo_todos | ✅ |
| 学习报表 reports | tab | reports.js | reports | ✅（待真机数据校验）|
| 我的 mine | tab | app.js | — | ✅（政治/专业课占位）|
| 单词背诵 vocab | 首页入口 | english/vocabulary.js + dict.js + freq.js | vocab_words/vocab_known(分片)/vocab_plan/learn_progress/daily_checkin/vocab_streak | ✅ |
| 文章阅读 articles | 首页入口 | english/articles.js | articles | ✅（阅读答题占位）|
| 长难句 sentences | 首页入口 | english/sentences.js | sentences | ✅ |
| 作文模板 essay | 首页入口 | english/essay.js | essays | ✅ |
| 题库 math-bank | 首页入口 | math/question-bank.js | math_questions | ✅ |
| 薄弱错题 math-weak | 首页入口 | math/weak-points.js | math_weak_points | ✅（依赖AI写入）|
| 好友叫醒 friend-wake | 首页入口 | friend-wake.js + WakeStore | WakeBind/WakeMsg | ✅功能（权限引导缺）|
| 宠物小窝 pet | 首页卡/桌面宠物 | pet.js + pet-ui.js | pet_data/coins/coin_log/pet_pos | ✅ |
| 倒计时 countdown | 首页Hero | countdown.js | countdown | ✅ |
| 每日一句 daily-quote | 首页卡 | daily-quote.js | （本地轮播offset）| ✅ |
| 设置 settings | 我的→设置 | app.js + config | settings_<user>（仅本地）| ✅ |

### 整体进度
- 主体功能全部上线可跑；当前阶段为质量打磨 + 少量未完模块补齐。
- 测试账号（123/1234/kaoyan2026/15330091616）云端数据**已全部清空**（AppData/WakeBind/WakeMsg/_User 均 0 残留），进入干净交付预备态。

---

## ③ 当前未完成清单（写到一半 / 待调试 / 待实现）

### 3.1 好友叫醒「权限引导轻量页」【待做】
- 现状：双向互绑逻辑已完成（commit 8efc803）。原生 Android 叫醒服务齐全（WakeGuardService 前台轮询 + WakePlayerService 响铃震动 + WakeAlarmActivity 锁屏页）。
- 卡点：华为/OPPO 等需手动开「自启动 / 电池不受限 / 后台弹出界面」，否则后台/锁屏叫醒不响。之前权限向导已回退，**目前 App 内没有引导用户开权限的页面**。
- 中断位置：权限向导代码曾写又撤，现无对应 UI。
- 接续：在 friend-wake 页面加一个「权限设置引导」卡片/弹窗，按机型列出需开启的开关（华为必开自启动+电池白名单+受保护应用；OPPO 必开自启动+后台运行+后台弹出界面），给跳转系统设置的 hint。

### 3.2 英语阅读「阅读答题」功能【占位】
- 现状：`articles.js` 阅读工具栏「阅读答题」按钮 `reader-quiz` → `startQuiz()` 仅 `Utils.toast('阅读答题功能开发中...')` 占位。
- 卡点：未实现（AI 已能拆出 questions[]，前端可基于 questions 出交互答题，但 UI/计分未做）。
- 接续：用 AI 解析返回的 `questions[]`（题干+选项+答案+解析）渲染答题卡，提交后判分并标注错题（可联动 math_weak 思路）。

### 3.3 RAG 知识库接入【硬性需求 · 未开工】
- 现状：当前 AI 答疑/解析全部走 qwen 直连 + prompt 约束，无任何检索增强。
- 需求：规划要求接入考研资料库（真题库/词汇库/专业课资料）做 RAG 检索增强，让 AI 回答有依据、可溯源。
- 卡点：未立项、未选型（向量库/分块/检索链路均未定）。
- 接续：先定知识库来源与切片策略，再选向量方案（Bmob 不适合向量，需外部或自建），在 `ai-assistant.js` 的 qwen 调用前插入 retrieve→inject 步骤；注意浏览器走 aiProxy 的 40KB 限制。

### 3.4 数学题 / 文章 双端同步真机实测【待验】
- 现状：Store 同步代码已接入（双写本地+云端，登录后生效）。
- 卡点：仅在逻辑层验证，未在两台真机/两端交叉实测过「一端改、另一端拉取合并」的完整闭环。
- 接续：用两个账号在两设备互改数据，验证 `Store.getUserData` 的云端优先 + 本地独有回传逻辑无丢数据/重复。

### 3.5 政治模块 / 专业课模块【占位】
- 现状：「我的」页有 `即将上线` 占位按钮，点击弹 toast。
- 卡点：未开发（无数据表、无页面）。
- 接续：按现有模块范式（独立 section + Store 表 + 首页入口）新建。

### 3.6 设置类数据不跨端【已知限制】
- 现状：`settings_<user>`（每日专注目标、AI设置、背词翻页速度等）仅 localStorage，不跨端。
- 卡点：未接入 Store 云端同步（按用户隔离但不跨设备）。
- 接续：若需跨端，改走 `Store.put('settings', {...})` 即可，与 pet_pos 同思路。

### 3.7 交付前收尾【待办】
- Revoke git remote 明文 GitHub token（本地 `git remote -v` 可见，含 token）。
- 清本机测试机 IndexedDB 本地缓存（设置→清除所有数据，或重装）。
- 若重新启用 SW 做 PWA 离线，必须 bump `www/sw.js` 的 CACHE（见 ④.7）。

---

## ④ 历史约束要求（硬性规则 · 不可违反）

### 4.1 代码风格
- 变量/方法用 ASCII 引号；JSON 键双引号；改写 JS 后必须 `node --check` 防语法错误。
- **不裸露原生控件**：`<input type="date|time|month|week|number|range|color">` 和 `<select>` 一律用 `widgets.js` 的 `data-date-pill` / `data-custom-select` 钩子（Android WebView 会弹系统 UI 违和）。
- 图标统一线性 SVG（stroke 1.75, 24x24, fill none）；全站 Emoji 已清零（用 SVG 形状代替粒子）。
- 改任意 JS/CSS 后，必须 bump `index.html` 里对应 `<script>/<link>` 的 `?v=` 戳（见 ④.7）。

### 4.2 互通架构（数据）
- Bmob 数据按 `Bmob.dataUserId`（规范名）存/取，**禁直接用 `Bmob.username`**（跨设备拆库 bug 已修）。
- 图片不存云端，只同步识别后文字。
- 业务数据走 `Store.put/get`：云端 AppData + 本地 IndexedDB 双写（前提 `Bmob.isLoggedIn()`，否则仅本地）。
- 好友叫醒走独立 `WakeStore` → Bmob `WakeBind`/`WakeMsg` + 本地缓存（与 Store 平行的另一条同步链路，绑定关系/叫醒消息均跨端）。
- 未接入云端：设置类 `settings_<user>`、每日一句轮播 offset（仅 localStorage，非用户数据）。

### 4.3 AI 代理架构铁律（英语识别核心）
- **两段式管线**：Stage1 `qwen-vl-max` 只逐字抄写（max_tokens 6000, temp 0.1，禁出 JSON）→ Stage2 `qwen-max` 拆 `{summary, article, questions[]}`；article 须剔题目/题号/选项，explanation 禁抄文章段落。
- 长图（高/宽比>1.4）竖向切分；`≤2` 张先切再压，`>2` 张不切；比例 1.4–2.4 切2段、≥2.4 切3段（`Utils.splitLongImage`）。
- **平台自适应预算**：App 原生 `MAX_TOTAL=220KB`，浏览器 `36KB`（aiProxy 限制）。
- **失败必有兜底**：JSON 失败→正则 `\d+[.、]` 抓题；彻底失败→显示 raw 折叠区；禁止空白页。article 仅取 `data.article`（禁 `|| art.content` 兜底，否则题目会灌进文章区）。
- **分段三层兜底**：后端 `_cleanArticleText`（删页脚/合并空行/零空行按句切）+ 前端 `renderReaderContent` 按空行 split + 视觉 `text-indent:2em`（已修 781462d）。

### 4.4 界面规范
- 背景：蓝天白云图 + 浅蓝兜底（低饱和渐变允许，不裸渐变）。
- mobile `content-section` padding **必须拆分写**（基类 padding-bottom 含 tabbar 安全间距），不能用 `padding:14px` shorthand 覆盖，否则底部无 tabbar 间距被遮挡（历史坑 69b009a）。
- 桌面元素 z-index：tab=1000 / 通用模态=10000 / 桌面宠物=9998 / 宠物panel=10010 / toast=10020。
- AI 解析区不渲染文章正文（阅读区已渲染）；题干加粗 `.ai-q-text{font-weight:700}`；选项独立 `<ul>`。
- `display:flex` 须限定 `.content-section.active`；勿动全局 html/body overflow。

### 4.5 RAG 知识库（硬性需求 · 设计约定）
- 目标：AI 答疑/解析须基于考研资料库检索增强，回答可溯源，而非纯靠模型记忆。
- 当前状态：未开工（见 ③.3）。
- 接入时硬性约束：检索上下文注入必须在 qwen 调用**之前**；浏览器走 aiProxy 受 40KB 限制，检索结果需截断；不得因加 RAG 而退化现有「失败兜底」铁律（④.3 末条）。

### 4.6 不允许随意简化逻辑
- 识别不全/解析失败不得用「空结果」或「静默跳过」代替；必须有可见兜底（正则/raw/toast）。
- 长文识别不得为省 token 过度压缩导致丢字（曾翻车，已定 App 220KB 预算）。
- prompt 约束必须对应「过去翻车过的具体形态」，不写空话（如题目特征词清单来自真题常见 What/Which/Why/How/According to）。
- 任何「为了省事合并段落/跳过校验」的改法都禁止。

### 4.7 构建 / 缓存 / SW 规矩（已校准）
- **Service Worker 当前已禁用**：`index.html` 末尾脚本每次启动主动 `unregister` 旧 SW + 清 `caches`（调试阶段防加载旧版 JS）。`www/sw.js` 仍在，但未被 register。
- **因此当前缓存破除机制 = `index.html` 里每个 `<script src>`/`<link href>` 的 `?v=` 查询戳**。改完 JS/CSS 只 bump 对应戳即可生效，**不用动 sw.js**。
- **若未来为 PWA 离线重新启用 SW**：必须同步 bump `www/sw.js` 的 `CACHE` 常量（v1→v2），否则旧 CSS/JS 残留诡异 bug（`activate` 钩子会清旧缓存）。
- 二进制资源（GIF/图片/字体）必须随代码 commit 进 `www/对应目录`，光 commit JS/CSS 不够，APK 不装。
- 本地验证：`python serve.py`（端口 8765），**必须用 `http://localhost:8765` 打开**，双击 `file://` 会被浏览器拦跨域、云端全失效（index.html 已内置 file:// 拦截提示页）。

---

## ⑤ 遗留问题（中断原因 + 必须规避的坑）

### 5.1 常见中断/翻车原因
- 长卷图下半内容丢失：曾把 App 也限到浏览器 36KB → 改为平台自适应 220KB（④.3）。
- 不分段：VL 默认「接续输出」不分段 → prompt 强约束段间空行 + 三层兜底（已修）。
- 旧 CSS 残留：曾因 SW 旧缓存 + 旧 `themed[page]||'default'` 让宠物页变奶黄 → 现已禁用 SW，靠 `?v=` 戳。
- 测试账号脏数据：曾脚本循环灌 500 条 WakeMsg 触发疯狂震动 → 已清，且 WakeMsg 灌数据属禁忌。

### 5.2 必须规避的坑（排障速查）
- **APK 下载**：Azure 302 重定向 URL 自带 SAS，**必须剥离 Authorization**（PowerShell `Invoke-WebRequest` 转发 GitHub token 到 Azure 会 403）；Bash/curl/python 走沙箱会被掐断 Azure 下载，改用 PowerShell 直连。
- **artifact 命名**：每个 run 有 2 个 artifact（`gradle-build-log` ~75KB 假包 vs `kaoyan-study-helper-debug-apk` ~11MB 真包），必须按 name 命中后者，否则下到假包。
- **SW 缓存**：现 SW 禁用，靠 `?v=`；若启用 SW 则必 bump `sw.js` CACHE（④.7）。
- **二进制 commit**：GIF/图/字体不随代码 commit → APK 破图/不装。
- **长文识别**：VL 边看图边出结构化 JSON 必丢字 → 必须两段式（VL 只抄写）。
- **段落**：VL 默认不分段 → prompt 显式「段间空行」+ 前后端双层兜底；零空行时 `_cleanArticleText` 按句切（英文 `[.!?]\s+[A-Z][a-z]`、中文句号/换行）。
- **页脚过滤**用整行正则 `^...$`，勿用全文 substring，避免误伤正文中出现的「2007 年考研试题」字样。
- **mobile padding** 改完用拆分写法，防 tab 栏遮挡。
- **不裸露 date/select**：Android WebView 会弹系统 UI，用 widgets.js 控件。
- **测试账号清理后**：本机 IndexedDB 仍留旧缓存，正式发布前清一次。

---

## ⑥ 接续引导（新会话直接从此继续）

读取本文后，无需重新理解需求，按下列清单维护/开发：

### 维护 / 调试速查
1. **本地起服务**：`cd 考研学习助手 && python serve.py` → 浏览器开 `http://localhost:8765`（勿用 file://）。
2. **改 JS/CSS 后**：bump `index.html` 对应 `?v=` 戳 → `node --check` 改过的 JS → `git commit` → `git push origin main`。
3. **取 APK**：push 触发 `build-apk.yml`；run 完成后按 name 命中 `kaoyan-study-helper-debug-apk` 下载（PowerShell 剥离 auth 下 Azure），落到桌面。
4. **排障先看**：① 是否 localhost 打开；② `?v=` 戳是否 bump；③ 是否登录（同步前提）；④ 控制台报错；⑤ 安卓权限（叫醒类先看自启动/电池白名单）。

### 下一步开发优先级（按 ③ 顺序）
1. 【3.1】补好友叫醒权限引导页（最高性价比，用户已多次踩坑）。
2. 【3.2】实现英语阅读答题（数据已有，纯前端 UI）。
3. 【3.4】双端同步真机实测闭环。
4. 【3.3】RAG 知识库立项（需求硬，但量大，建议单独排期）。
5. 【3.5/3.6】政治/专业课模块、设置跨端（按需）。

### 每次实质改动后
- 追加 `C:/Users/yansh/WorkBuddy/考研软件/.workbuddy/memory/YYYY-MM-DD.md` 工作日志。
- 长期规则/架构变更同步 `MEMORY.md`（本案已修正「SW 已禁用」一条，旧记忆的「必 bump sw.js」已不准确）。

### 关键文件地图（排障定位用）
- 入口/路由：`www/index.html`（页面结构+脚本加载+?v戳+SW禁用脚本）、`www/js/app.js`（navigate/首页挂载/设置）。
- 数据层：`www/js/store.js`（Store 双写）、`www/js/bmob.js`（Bmob 封装）、`www/js/friend-wake.js`+WakeStore（叫醒同步）。
- AI：`www/js/ai-assistant.js`（两段式管线/兜底/上传归档）、`www/js/config.js`（API Key 配置）。
- 英语：`www/js/english/{vocabulary,dict,freq,articles,sentences,essay}.js`。
- 数学：`www/js/math/{question-bank,weak-points}.js`。
- 宠物：`www/js/pet.js`+`www/js/pet-ui.js`；专注：`www/js/pomodoro.js`；倒计时：`www/js/countdown.js`；每日一句：`www/js/daily-quote.js`；报表：`www/js/reports.js`；控件：`www/js/widgets.js`；工具：`www/js/utils.js`。
- 原生：`android/app/src/main/java/com/kaoyan/studyhelper/`（MainActivity + plugins/{CameraPlugin,Wake*}）。
- 构建：`build-apk.yml`、`deploy-pages.yml`（push main 触发）；`www/sw.js`（启用 SW 时才需 bump）。
- 凭据：**index.html 内 `APP_CONFIG.bmob` 含 appId/restKey（公开，已在代码）**；Master Key 不在代码，仅 Bmob 控制台/手动操作 _User 时用。

---

## ⑦ 变更记录（2026-08-06 · Codex 工作副本）

- 登录不再自动建号：`auth.js` 移除「登录失败 → 注册试探」逻辑及 `_fixCloudAccount`/`_ensurePet`，登录失败统一提示账号或密码错误。
- 日期统一中国本地日（UTC+8）：`Utils.today/thisWeek/shiftDate/cnDate/formatDate` 重写；`store.js` 今日数学/AI 统计、`reports.js` 日报/周报、`vocabulary.js` 周起始与补签全部改用中国日历日期。
- 修复 P1：`pomodoro.js` 近 7 天图 `pts.indexOf` 恒 -1 崩溃（改为按下标取 `days[i]`）。
- 修复重复绑定：各模块 `init()` 增加 `_bound` 幂等保护，退出再登录不再重复 addEventListener。
- 修复新登录后桌面宠物不挂载：`app.initAllModules()` 登录后调用 `PetUI.mount()`。
- 健壮性：列表渲染标题/原文/来源统一 `Utils._escapeHtml`；`auth.js` 不再打印 sessionToken；`app.js` 导出/清空覆盖全部 Store 表 + 好友叫醒 WakeBind/WakeMsg；`ai-assistant.js` 上传归档薄弱点 questionId 与题目 id 对齐、>2 张图不再强制切分；`articles.js` 翻译金币提示改为读实际配置；`pet.js` 补 expBuffMs 商店描述。
- 缓存：`index.html` 相关脚本 `?v=` 统一 bump 到 `20260806h`。

### ⑧ 第二批修复（2026-08-06）

- 并发/数据一致性：`store.js` 金币读写加串行锁；`bmob.js` 保存按 module+id 串行化避免同设备重复行；`pomodoro.js` 待办跨端合并改为云端副本优先。
- 会话/缓存：`auth.js` 启动时 `Bmob.checkSession()` 校验本地会话，401/403 清会话回登录页；`reports.js` 报表缓存加 `_REPORT_VERSION=2`，旧日期口径缓存自动失效重生成。
- 叫醒：`friend-wake.js` 新增 `WakeStore.cleanup()`，定期（15 分钟节流）清理 3 天前的 WakeMsg，发送次数无上限；邀请码生成前查重（最多重试 5 次）。
- UI/占位：`app.js` 专注目标改用自定义弹层（弃用原生 prompt）；`articles.js` 阅读答题已实现（基于 AI questions 出题、判分、写入 wrongQuestions）；好友叫醒页新增权限设置引导卡及 dnd-tip；全站用户可见 Emoji 清理（toast/通知/Java 通知标题）；`manifest.webmanifest` theme_color 对齐实际主色 `#4A90B8`。
- AI：`ai-assistant.js` 上传归档压缩提质（1400px/0.85）并按平台自适应预算（App 220KB / 浏览器 36KB）。
- 缓存：相关脚本/CSS `?v=` bump 到 `20260806i`。

### ⑨ 第三批修复（2026-08-06）

- `bmob.js`：`saveAppData` 保存后追加 `_dedupeAppData()`，把跨设备并发产生的同 key 重复云端行收敛为最新一条。
- `store.js`：云同步写/删失败不再静默，30 秒节流 toast 提示“云端同步失败，数据已保存在本机，联网后会自动重试”。
- `server.js`：标注为已废弃（好友叫醒走 Bmob 轮询），`BUILD.md` 后端部署章节同步改为无需自建 WebSocket 后端。
- `friend-wake.js`：在 3 天保留清理之外增加 `_capWakeMessages()`，每方向 WakeMsg 最多保留 100 条（只删 1 小时前超上限的最旧记录），彻底限制云端行数增长。
- `pomodoro.js`：任务增加 `updatedAt`，跨端合并改为“更新更晚者胜”，本地新副本会被回传云端，消除云端旧副本覆盖刚编辑任务的竞态。
- 缓存：`bmob/store/friend-wake/pomodoro` 脚本 `?v=` bump 到 `20260806j`。
